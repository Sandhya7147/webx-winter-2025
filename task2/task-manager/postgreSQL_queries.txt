CREATE TABLE users( id SERIAL PRIMARY KEY, username VARCHAR(50) UNIQUE NOT NULL, password_hash TEXT NOT NULL);
CREATE TABLE tasks( id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL, title TEXT NOT NULL, completed BOOLEAN DEFAULT FALSE,  CONSTRAINT fk_user FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE );
 ALTER TABLE users ADD COLUMN role VARCHAR(10) DEFAULT 'user';